all the memlib is to be included with "nq_memlib/my_header.h"

Add the domains you want in domains_decl.h file with the line:
  NQ_USR_DOMAIN(MyDomain)
include "domains.h" when you want to have acces to your domains
as a type

define :
-WITH_NQ_MEMLOG to activate the logging with domains
-WITH_NQ_LOGTIME to display the time in hour/min/sec of your log
    (beware recovering the time makes allocations)
